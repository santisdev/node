const hobbies = ["Sports", "Cooking"];

hobbies.push("Programming");

console.log(hobbies);

// I can edit the array values
// even though is a const
// This is because, the const is referencing an
//address an that does not change if we add a new value
